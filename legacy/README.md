# Legacy static prototype

This folder keeps the pre-Next.js static HTML/JS prototype for reference only.
Production routes are served from the Next.js App Router under `app/`.

`legacy/` is excluded from Vercel deploys via `.vercelignore`.
